---
- name: Converge
  hosts: all
  gather_facts: false
  tasks:
    - name: "Include organicveggie.resilio_sync_docker"
      ansible.builtin.include_role:
        name: "organicveggie.resilio_sync_docker"
